import { FiltroSolbeneficiosPipe } from './filtro-solbeneficios.pipe';

describe('FiltroSolbeneficiosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSolbeneficiosPipe();
    expect(pipe).toBeTruthy();
  });
});
